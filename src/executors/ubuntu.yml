description: >
  This is the ubuntu executor

machine:
  image: ubuntu-<<parameters.version>>
parameters:
  version:
    default: 2004:202010-01
    description: >
      Pick a specific ubuntu image variant:
      https://circleci.com/docs/2.0/configuration-reference/#available-machine-images
    type: string
